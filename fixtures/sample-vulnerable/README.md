# sample-vulnerable

An **intentionally insecure** mini-repo used as the zero-config demo target for
Relix-Q OSS. Every file here contains deliberately weak, quantum-vulnerable, or
otherwise broken cryptography so a fresh `docker compose up` produces a
non-empty, deterministic scan result.

**Do not copy any of this code into a real project.**
